@model Oficial4.Models.Usuario

@{
    ViewBag.Title = "Deletar";
    var usrSession = Session["nome_Usuario"];
    if (usrSession != null && usrSession.ToString() != "adm")
    {
        Response.Redirect("~/Home/Login");
    }
}

<h2>Deletar</h2>

<h3>Tem certeza que deseja excluir este usuário?</h3>
<div>
    <h4>Usuário</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nome_Usuario)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nome_Usuario)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.senha_Usuario)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.senha_Usuario)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Excluir" class="btn btn-danger" /> |
            @Html.ActionLink("Voltar", "Index")
        </div>
    }
</div>
